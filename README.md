# Obsidian ドキュメント（ルート）

このフォルダ（`Documents`）が、画像用スクリプトから見た **ボールトのルート** です（`attachments/` と `brain/` の親）。

- **ノートの本体**: `brain/`（Obsidian の保管庫として開く場所）
- **構成の説明**: `brain/フォルダ案内.md`
- **補助ツール**: `tools/`（画像集約は `tools/vault-image-utils/`）

Cursor 用のルールは `.cursorrules` です。
